# Digital Twin Q-Learning Recommender (FastAPI)

Serve Q-learning recommendations from a saved Q-table ('models/best_q_agent.pkl') to a simple Bootstrap form.

## 1) Install
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
